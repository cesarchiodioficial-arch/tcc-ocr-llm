[pytest]
testpaths = tests
addopts = -q --cov=app --cov-report=term-missing
markers =
    llm_real: teste que exige LLM_API_KEY real (desativado por padrão)
    ocr_real: teste que exige Tesseract instalado no ambiente
